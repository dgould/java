/* SchemeApplet.java1234567890123456789012345678901234567890123456789012345678901234567890123456789*/package jscm;import java.awt.*;import java.applet.Applet;public class SchemeApplet extends Applet{	private SchemeInteraction	interaction;		private Checkbox			debug;	private Button				resetButton;	private Button				evalButton;		public void init()	{		this.setBackground(Color.white);				debug = new Checkbox("Debug Information");		resetButton = new Button("Reset Interpreter");		evalButton = new Button("ReadEvalPrint");				interaction = new SchemeInteraction(22, 60, debug);//		interaction.setFont(Font.getFont("Courier"));		interaction.setEditable(true);		interaction.setBackground(Color.white);		this.add(interaction);				this.add(debug);		//		resetButton.setBackground(Color.white);		this.add(resetButton);//		evalButton.setBackground(Color.white);		this.add(evalButton);//		interaction.Prompt();	}		public boolean action(Event evt, Object what)	{		if(evt.target == resetButton)		{			interaction.Reset();			return true;		}		if(evt.target == evalButton)		{			interaction.appendText("\n");			interaction.ReadEvalPrint();			return true;		}				return super.action(evt, what);	}		public boolean keyUp(Event e, int key)	{		if(interaction.keyUp(e, key))		{			return true;		}				return super.keyUp(e, key);	}		public boolean keyDown(Event e, int key)	{		if(interaction.keyDown(e, key))		{			return true;		}				return super.keyDown(e, key);	}}